# ====== HUGGING FACE CONFIGURATION ======
# Get your token from: https://huggingface.co/settings/tokens
# Create a READ token for the model inference
HUGGINGFACE_API_TOKEN=hf_your_token_here
HUGGINGFACE_MODEL=Qwen/Qwen2.5-7B-Instruct

# ====== DATABASE CONFIGURATION (MariaDB) ======
# These credentials are used by both the backend and MariaDB container
# Change these to secure values for production!
MARIADB_HOST=db
MARIADB_PORT=3306
MARIADB_DATABASE=seismic_db
MARIADB_USER=seismic_user
MARIADB_PASSWORD=seismic_password_2025
MARIADB_ROOT_PASSWORD=root_password_2025

# ====== BACKEND API CONFIGURATION ======
# USGS API endpoint for earthquake data
USGS_API_URL=https://earthquake.usgs.gov/fdsnws/event/1/query
# How often to poll USGS for new events (in seconds)
POLLING_INTERVAL_SECONDS=180
# FastAPI server host and port (inside Docker)
FASTAPI_HOST=0.0.0.0
FASTAPI_PORT=8000

# ====== INTERNAL DOCKER NETWORKING ======
# Used for container-to-container communication inside Docker
# These use the Docker service name "backend" which only works within the Docker network
BACKEND_INTERNAL_URL=http://backend:8000
BACKEND_PORT=8000

# ====== CORS CONFIGURATION ======
# Allowed origins for CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://frontend:3000,http://localhost:3001,http://backend:8000

# ====== FRONTEND CONFIGURATION (Next.js) ======
# For Docker: Use the backend service name (it works inside Docker network)
# For Local Development: Use http://localhost:8000
NEXT_PUBLIC_API_URL=http://backend:8000

# ====== MOBILE APP CONFIGURATION (Expo) ======
# CRITICAL: These use your PC's IP address, NOT Docker service names!
#
# WHY? The mobile app runs on your phone (outside Docker network) via Expo Go,
# so it cannot resolve Docker service names like "backend". It needs your PC's IP.
#
# IMPORTANT: Replace 192.168.1.126 with YOUR PC's IP address!
# Find your IP with: ipconfig (Windows) or ifconfig (macOS/Linux)
# Look for the IPv4 address under "Ethernet adapter" or "Wireless adapter"
#
# Example: If your IP is 192.168.1.150, change all occurrences of 192.168.1.126 to 192.168.1.150
#
EXPO_PUBLIC_API_URL=http://192.168.1.126:8000/api
EXPO_PUBLIC_WEBSOCKET_URL=ws://192.168.1.126:8000/api/ws
